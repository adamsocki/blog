{
  "name": "blog",
  "version": "1.0.0",
  "description": "Simple blog and portfolio for building in public",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
